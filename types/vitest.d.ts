// Registers the jest-dom matcher types (toBeInTheDocument, toHaveTextContent,
// …) on Vitest's Assertion interface. The runtime side is set up in
// vitest.setup.mts; this file is what makes `tsc --noEmit` accept the matchers.
/// <reference types="@testing-library/jest-dom/vitest" />

export {};
